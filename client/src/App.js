import { useState } from "react";

function App() {
  const [notes, setNotes] = useState(null);

  const fetchNotes = () => {
    //Fetch the notes
    //Set it on state
  }


  return <div className="App">Hello</div>;
}

export default App;
